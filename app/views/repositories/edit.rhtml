<h2><%= l(:label_repository) %></h2>

<% labelled_tabular_form_for :repository, @repository, :url => repository_path(@path), :html => {:method => :put} do |f| %>
  <%= render :partial => 'form', :locals => {:f => f} %>
<% end %>
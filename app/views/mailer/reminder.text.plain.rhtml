<%= l(:mail_body_reminder, :count => @issues.size, :days => @days) %>:

<% @issues.each do |issue| -%>
  * <%= "#{issue.due_date.strftime '%Y-%m-%d'} - #{issue.project} - #{issue.tracker} ##{issue.id}: #{issue.subject} - #{issue_url(issue)}" %>
<% end -%>

<%= @issues_url %>
